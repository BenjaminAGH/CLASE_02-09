
const persona = {
    nombre: 'Benjamin',
    edad: 21,
    genero: 'Masculino'
}

console.log(persona.nombre + " tiene "+ persona.edad + " y es de genero " + persona.genero);

const caja = {
    cuadernos: true,
    lapices: false,
    papel: true,
    fotografias: false,
}

console.log("En la caja hay cuadernos: " + caja.cuadernos + " es de tipo " + typeof(caja.cuadernos) + "hay lapices: " + caja.lapices + " es de tipo " + typeof(caja.lapices) + " " + "hay papel: " + caja.papel + " es de tipo " + typeof(caja.papel) + " " + "hay fotografias: " + caja.fotografias + " es de tipo " + " " + typeof(caja.fotografias));